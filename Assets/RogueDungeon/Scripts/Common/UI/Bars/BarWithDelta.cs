using UnityEngine;
using Zenject;

namespace Common.UI.Bars
{
    // public abstract class BarWithDelta : Bar
    // {
    //     [field: SerializeField] public Bar _deltaBar;
    //
    //     [Inject]
    //     public void Construct(BarDeltaConfig deltaConfig, IBarViewModel viewModel) => 
    //         _deltaBar?.Construct(new BarDeltaViewModel(viewModel, deltaConfig), deltaConfig);
    // }
}